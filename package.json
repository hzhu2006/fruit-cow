{
  "name": "fruit-cow",
  "version": "1.0.0",
  "private": true,
  "description": "Editable online milk tea ordering website for Fruit Cow.",
  "scripts": {
    "start": "python3 -m http.server 8000 --bind 0.0.0.0",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
